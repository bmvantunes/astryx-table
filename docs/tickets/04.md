# 4. Keyboard and recycled-focus parity

## What to build

Header/body navigation and overlays remain coherent as cells recycle.

## Acceptance criteria

- [ ] Preserve Active Cell, one tab stop and minimum-delta reveal with empty and recycled rows.
- [ ] Verify pointer/context/keyboard menu activation, submenus, nested Escape, filter transfer and connected-trigger/unmount fallback.
- [ ] Use TanStack-owned shortcut matching; retain editor/IME and descendant focus ownership.
- [ ] Exercise public Browser tests with nested grids, portals and virtual unmount.

## Blocked by

- Draft ticket 2: Virtual read-only Client with Astryx theme and menu

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
