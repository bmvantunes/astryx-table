# 7. Server viewport and source lifecycle

## What to build

A Server grid consumes authoritative sparse rows and presents source lifecycle correctly.

## Acceptance criteria

- [ ] Preserve generation replacement versus same-generation windows, overlapping cache and late-delivery rejection.
- [ ] Use source row keys; never expose Server getRowId or pagination and never reconstruct identity from values.
- [ ] Present loading skeletons and retained stale/error/closed rows, optional Retry owned by source.
- [ ] Verify real adapter integration, segmented windows and source-update performance; Effect remains optional at root.

## Blocked by

- Draft ticket 2: Virtual read-only Client with Astryx theme and menu

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
