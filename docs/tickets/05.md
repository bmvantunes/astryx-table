# 5. Filtering and persisted preferences

## What to build

Exact filters, non-empty sorting and sanitized durable preferences work through Astryx controls.

## Acceptance criteria

- [ ] Retain runtime value operands, codecs, half-open ranges, Match None and quick-filter session semantics.
- [ ] Support existing Set/text/numeric/boolean filter workflows, column visibility/order/pinning/widths and sort controls.
- [ ] Persist only allowed preferences; explicitly decide handling of old storage keys rather than blind rename.
- [ ] Retain inference tests and Browser restoration/filter focus regressions.

## Blocked by

- Draft ticket 2: Virtual read-only Client with Astryx theme and menu
- Draft ticket 4: Keyboard and recycled-focus parity

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
