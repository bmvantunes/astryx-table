# 6. Read-only Client grouping

## What to build

Client groups, summaries and controls retain identity and presentation semantics.

## Acceptance criteria

- [ ] Preserve separate normal/group sorting, exact aggregates, Rows System Column and width persistence.
- [ ] Keep grouped callbacks free of fabricated raw rows and preserve typed Group Key/Aggregate context.
- [ ] Clear raw selection and active range on shape transitions; reconcile focus by grouped identity during live updates.
- [ ] Verify grouping add/remove/reorder keyboard announcements and copied grouped values.

## Blocked by

- Draft ticket 5: Filtering and persisted preferences

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
