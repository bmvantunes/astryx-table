# 8. Server filters and grouping

## What to build

Exact queries, facets and grouped results work end-to-end through the source adapter.

## Acceptance criteria

- [ ] Preserve exact filter codecs, Match None, non-empty sorts and independent grouped sorting.
- [ ] Keep source-owned aggregate aliases/row identity behind the adapter; no sparse-cache aggregation.
- [ ] Verify lifecycle/query generation during filter/group changes, including obsolete responses.
- [ ] Retain typed raw/group/aggregate presentation tests and Server Browser acceptance.

## Blocked by

- Draft ticket 5: Filtering and persisted preferences
- Draft ticket 7: Server viewport and source lifecycle

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
