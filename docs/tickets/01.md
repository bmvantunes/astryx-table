# 1. Audited core import and AstryxTable rename

## What to build

A traceable, non-published migration baseline that retains the existing domain behavior and tests.

## Acceptance criteria

- [ ] Pin a merged source commit and explicitly disposition known PR #100 findings; no dirty checkout copy.
- [ ] Record included/excluded files, source/target hashes and deterministic rename rules; retain notices and historical provenance.
- [ ] Import all relevant domain/specification/ADR and test/benchmark coverage into a parity ledger; reconcile new branding and UI ownership.
- [ ] Verify normalized transformed core matches source; run retained domain and type tests. Mechanical-only review exemption does not cover semantic fixes.
- [ ] Any temporary old renderer remains isolated and non-shipping; no legacy UI package becomes a successor consumer dependency.

## Blocked by

None (can start immediately).

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
