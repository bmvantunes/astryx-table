# 3. Pinned, RTL, resize and reorder parity

## What to build

Pinned partitions and column-management gestures work in the new renderer.

## Acceptance criteria

- [ ] Retain start/center/end logical order, measured insets and narrow-layout suspension before/after measurement.
- [ ] Keep horizontal and vertical reveal minimal and segmented-rowspace correct in LTR/RTL.
- [ ] Verify resize/reorder persistence, pointer cancellation and keyboard commands without duplicate generic handlers.
- [ ] Run real pinned/two-axis/custom-renderer performance tests with unchanged bounds and accounting.

## Blocked by

- Draft ticket 2: Virtual read-only Client with Astryx theme and menu

## Verification

Use public Client/Server behavior and installed-consumer seams where applicable. Retain existing domain/type tests and benchmark invariants. The mechanical-core exemption applies only to unchanged imported logic and deterministic rename, not this ticket's new behavior.
